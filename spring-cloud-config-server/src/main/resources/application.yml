
server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          #配置git仓库地址
          uri: https://github.com/kdso/spring-cloud-config
          #配置仓库路径
          search-paths: config
      #配置仓库的分支
      label: master